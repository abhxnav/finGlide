'use client'

import Lottie from 'lottie-react'
import CardAnimation from '@/../public/assets/animations/cards.json'

const AuthAnimation = () => {
  return <Lottie animationData={CardAnimation} loop={false} />
}

export default AuthAnimation
